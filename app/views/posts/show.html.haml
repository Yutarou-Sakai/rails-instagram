.container
    .content_images
        -if @post.content_images.attached?
            - @post.content_images.each do |image|
                = image_tag image
    .content_body 
        = @post.content
